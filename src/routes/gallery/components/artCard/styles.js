import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    root: {
        minWidth: 345,
        maxWidth: 345
    },
    media: {
        height: 345,

    },
});

export default useStyles;